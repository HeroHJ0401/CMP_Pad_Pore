# CLAUDE.md

CMP 패드 SEM 이미지에서 개공률을 산출하는 데스크톱 프로그램. tkinter GUI + CLI,
PyInstaller로 Windows/macOS 실행 파일 빌드.

## 이 도메인에서 틀리기 쉬운 것

작업 전에 반드시 알고 있어야 하는 사실들. 여러 번 실수해서 얻은 것이다.

**개공률은 단순 암부면적률이 아니다.** 어두운 픽셀 비율을 그대로 쓰면 결론이
**반대 방향**으로 나온다. 어두운 영역에는 노출된 기공과 찢긴 리가먼트 사이의
그림자가 섞여 있어, 표면이 거칠수록 값이 커지는 거칠기 대리 지표가 되기 때문이다.
주 지표인 **유효 Pore 개공율**은 `solidity ≥ 기준인 Pore의 면적 합 ÷ 시야 면적`이다.
그림자 덩어리는 대체로 오목하므로 solidity로 걸러진다.

표에는 세 개의 개공율이 있고 **유효 + 무효 = Pore 개공율**이 항상 성립한다. 논문에
쓰는 값은 유효 Pore 개공율이다. Pore 개공율(전체)은 그림자가 섞여 있어 조건 간
비교에 쓰면 방향이 뒤집힐 수 있다.

**개공률은 픽셀 크기에 수학적으로 불변이다.** 분자와 분모가 모두 px²로 스케일되어
상쇄된다. 최소 등가직경 필터(µm)를 끄면 0.101 µm/px든 0.808 µm/px든 값이 같다.
픽셀 크기가 실제로 바꾸는 것은 등가직경·밀도·시야, 그리고 그 필터를 통과하는 객체의
범위뿐이다. 원형도와 solidity는 무차원이라 무관하다. 이걸 모르고 "픽셀 크기가 틀려서
개공률이 틀렸다"고 말하면 안 된다.

**같은 임계값이 같은 기준은 아니다.** 이미지 밝기가 다르면 고정 임계는 조건 간 비율을
무너뜨린다(합성 시험에서 2.25배 → 1.73배). 조건 비교에는 `대비 정규화 + Otsu(일괄)`을
쓴다. 이미지별 Otsu는 밝기 차이를 없애지만 조건 간 실제 차이도 일부 흡수한다.
일괄 Otsu는 비교 대상 전체의 밝기 분포에서 임계값 하나를 뽑아 모두에 적용한다.

**절대값은 불안정하다.** 임계값과 solidity 기준을 바꾸면 크게 움직인다. 대소 관계와
비율만 주장하고, 절대값을 쓸 때는 측정 조건을 반드시 병기한다.

**프레임 접촉 객체 제외는 개공률을 과소평가시킨다.** 큰 객체일수록 경계에 닿기 쉽다.
제외된 면적 비율(`접촉면적 %`)을 항상 함께 보고한다.

**시야 면적에 정보바나 레터박스가 들어가면 분모가 부풀어 값이 낮게 나온다.**
`접촉면적 %`가 비정상적으로 크면 크롭부터 의심한다.

## 검증 원칙

**수치 주장은 재현한 뒤에만 한다.** 이 저장소에서 "이 옵션이 더 낫다"는 주장을 한 번
했다가 다른 합성 이미지에서 성립하지 않아 철회한 적이 있다(µm 단위 σ). 테스트는
이미지 의존적 주장 대신 결정론적 성질을 검증하도록 쓴다 — "µm로 지정하면 모든
배율에서 σ의 물리적 크기가 동일하다"처럼.

**`python tests/smoke_test.py`가 모든 변경의 최소 관문이다.** 37개 항목이 도메인
불변식(스케일 불변성, 밝기 왜곡 재현과 복원, 크롭 정확도, 라벨 유일성)을 검사한다.
새 동작을 넣으면 항목을 추가한다.

**GUI 변경은 Xvfb에서 실제로 구동해 확인한다.** 정적 검사로는 안 잡히는 버그가 이미
여러 건 나왔다: 워커 스레드에서 Tk 변수를 읽어 생긴 교착, 툴팁이 뜨는 순간 `<Leave>`가
발생해 스스로를 지우는 문제, 레이블 wraplength를 컨테이너 폭에서 유도해 생긴 축소
피드백 루프.

```bash
xvfb-run -a python3 your_gui_test.py     # root.update() 루프로 구동
```

## 다중 파일 배치에서 확인할 것

- 실패한 파일 하나가 배치를 멈추면 안 된다. 모달은 끝에 한 번만.
- 폴더가 다른 동명 파일은 `core.unique_labels()`로 구분한다. 표·CSV·오버레이 파일명
  전부에 적용해야 한다(CLI `--overlay-dir`이 서로 덮어쓴 적 있음).
- 미리보기 오버레이는 축소해서 보관한다. 원본 해상도로 전부 들고 있으면 2048×1536
  기준 100장에 0.9 GiB가 된다. 저장할 때만 해당 이미지를 재계산한다.

## CI / 패키징에서 물린 것

`.github/workflows/build.yml`을 건드리기 전에 읽을 것.

- **macOS에는 `timeout` 명령이 없다.** GNU coreutils 전용이다. GitHub의 bash 단계는
  `bash -e`로 실행되므로 `command not found`가 곧 단계 실패다. `tests/run_timeout.py`를
  쓴다.
- **bash 단계의 `-e` 때문에 중간 명령 하나가 실패하면 그 자리에서 끝난다.** 진단용
  명령은 실패해도 계속되도록 감싼다.
- **Windows의 `--windowed` 빌드는 처리되지 않은 예외가 나면 모달 메시지 박스를 띄운다.**
  러너에는 닫을 사람이 없어 6시간 한도까지 매달린다. 실제로 35분 매달린 적 있다.
  잡과 단계에 `timeout-minutes`를 반드시 건다.
- **한국어 출력은 Windows 콘솔 기본 코드 페이지에서 `UnicodeEncodeError`를 낸다.**
  `main.py`의 `_ensure_console_io()`가 stdout을 utf-8로 reconfigure한다. 지우지 말 것.
- **scikit-image는 lazy loader를 써서 PyInstaller 정적 분석이 실패한다.**
  `--collect-all skimage`가 필요하다. 빌드가 깨지면 여기부터 본다.
- **macOS `.app`은 `zip -y`로 묶는다.** 심볼릭 링크가 깨지면 실행되지 않는다.
- 검증 단계는 **프리즈 전(`python main.py`)과 프리즈 후(실행 파일)를 모두** 돌려
  코드 문제와 패키징 문제를 구분한다.

## 로딩 표시

- 실행 파일이 자기 내부를 푸는 구간(파이썬 시작 전)은 PyInstaller의 `--splash`가
  덮는다. **Windows 전용이다** — macOS에서 이 플래그를 주면 빌드가 실패하므로
  워크플로에서 `RUNNER_OS`로 갈라 붙인다. 이미지는 저장소 루트의 `splash.png`.
  `pore_analyzer/splash.py`의 `_boot_splash()`가 자체 창이 뜨는 순간 닫고,
  CLI 모드에서도 닫는다(안 닫으면 창이 남는다).
- 시작 시: `pore_analyzer/splash.py`. **tkinter 말고는 아무것도 import하지 않는다.**
  numpy·scipy·skimage import 전에 창이 떠야 의미가 있기 때문이다. 실측 0.51초에
  표시되고 그 시점에 무거운 모듈은 0개였다. import는 워커 스레드에서 돌고 메인
  스레드는 Tk만 만진다.
- 분석 중: 진행 막대는 이미지 단위가 정해진 구간에서 determinate, 일괄 Otsu처럼
  길이를 모르는 구간에서 indeterminate로 바뀐다. `_Spinner`는 "멈춘 건 아닌가"에
  답하는 용도라 시작 전에는 아무것도 그리지 않는다.

## 릴리스

```bash
git tag v1.0.0 && git push origin v1.0.0   # Releases에 두 플랫폼 파일 자동 첨부
```

Actions 로그는 익명으로 읽을 수 없다(403). 실패 원인이 필요하면
`gh run view --log-failed`.

## 코드 관례

- UI 문구와 사용자 대상 메시지는 한국어, 존댓말. 코드 주석과 커밋 메시지는 영어/한국어 무관.
- 화면에 보이는 설명 문구는 `gui.py`의 "설 명 문 구" 배너 아래 한 곳에 모여 있다
  (`COL_HELP`, `PARAM_HELP`, `CHECK_HELP`, `MODE_HELP`). 사용자가 직접 고치는
  영역이므로 계산 코드를 그 안에 섞지 않는다. 열의 구조는 `COLUMNS`가 따로 갖는다.
- 새 열을 추가하면 `COLUMNS`와 `COL_HELP` 양쪽에 넣는다. 스모크 테스트가 둘의
  일치를 보지는 않으므로 직접 확인한다.
- `core.py`는 GUI 의존성이 없어야 한다. 헤드리스로 테스트 가능해야 한다.
- 기본값은 기존 논문 수치(고정 임계 0.45)를 재현하도록 유지한다. 더 나은 설정은
  `권장 설정` 버튼으로 제공하되 기본값을 바꾸지 않는다.
